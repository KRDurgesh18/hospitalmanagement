spring.application.name=hospitalmanagementssystem
# Server Configuration
server.port=8080

# Database Connection
# Note: Changed 'user_db' to 'hospital_db' for your current project
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=vijay27

# JPA / Hibernate Configuration
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create

# Optional but helpful: Shows the SQL queries in your console when you book/delete
spring.jpa.show-sql=true
